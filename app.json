{
  "name": "Philosophy Club",
  "scripts": {
    "postdeploy": "pg_dump $DATABASE_URL_FOR_CLONING | psql $DATABASE_URL && bundle exec rake db:migrate"
  },    
  "environments": {
    "test": {
      "scripts": {
        "test": "rake assets:precompile && bundle exec rspec"
      },
      "addons":[
        "heroku-postgresql"
      ]
    }
  }
}
