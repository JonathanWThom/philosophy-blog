{
  "name": "Philosophy Club",
  "scripts": {
    "postdeploy": "bundle exec db:schema:load db:migrate"
  },
  "environments": {
    "test": {
      "scripts": {
        "test": "rake assets:precompile && bundle exec rspec"
      },
      "addons":[
        "heroku-postgresql"
      ]
    }
  }
}
